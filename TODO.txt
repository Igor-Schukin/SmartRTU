1)Install doxygen on Pi+
2)create json for adverts +
3)Create folder for widget HtmlAds and implement it into project struct+
3)refacotring of htmlads widget
    3.1)parse json of adverts and put them into vector:
        what to do if json do not exist or corrupted
    3.2)rotating of adverts
    3.3)better advert shell class
        3.3.1)on destruction can:
                 kill cutycapt tthread,
                 delete .png if it exist
4)rotatation of adverts
5)detection if advets.json was edited if so total update
5) make total update

additional:
* find comands to get parameters like in top command
* run the infoboard in test mode for 4 hours after finishing 5 poitns